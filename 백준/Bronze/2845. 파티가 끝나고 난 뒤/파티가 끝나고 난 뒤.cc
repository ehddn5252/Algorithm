#include<iostream>

using namespace std;
int main() {

	int a, b,a1,a2,a3,a4,a5;
	cin >> a >> b;

	a *= b;

	cin >> a1 >> a2 >> a3 >> a4 >> a5;
	cout << a1 - a << " " << a2 - a << " " << a3 - a << " " << a4 - a << " " << a5 - a;
	return 0;
}