

#include<iostream>

using namespace std;


int main() {

	string a, b;
	cin >> a;
	cin >> b;

	if (a.size() >= b.size() ){
		cout<<"go";
	}
	else {
		cout<<"no";
	}
	return 0;
	
}